import React from 'react';
import HotelCart from '../Cart/HotelCart';

const PopularHotels = () => {
    return (
        <div>
            <HotelCart/>
        </div>
    );
};

export default PopularHotels;